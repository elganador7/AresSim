# Library Data

This folder contains the on-disk YAML source for the unit library.

- `default/`: built-in catalog shipped with the app.

Keep the file layout stable and machine-readable. Loader behavior assumes recursive discovery under this tree.
