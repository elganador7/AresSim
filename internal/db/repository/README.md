# Repository

This folder is reserved for DB repository helpers if persistence logic grows beyond the current small schema/client setup.

At the moment most DB access is still handled directly from the app bridge and manager layers.
