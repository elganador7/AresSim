# Engine Schemas

This subtree contains simulator protobuf schemas grouped by version.

Use `v1/` for the current contract. Add a new versioned folder only when a clean break is preferable to evolving the existing schema.
